Answers in seperate .py file.

1. **Reverse an Array**
   - Reverse chat messages (oldest → newest)
   - Timeline feeds
   - Undo history stacks
2. **Find kᵗʰ Largest Element**
   - Leaderboards (Top 10 users)
   - Analytics dashboards (top metrics)
   - Ranking systems
3. **Move Zeroes to End**
   - Clean API payloads
   - Filter empty form values
   - Data preprocessing before DB save
4. **Check Anagrams**
   - Input normalisation
   - Duplicate tag detection
   - Search keyword matching
5. **Reverse Linked List**
   - Undo/redo systems
   - Event sourcing log replay
   - Internal cache structures
6. **Detect Cycle in Linked List**
   - Detect circular dependencies
   - Prevent infinite redirects
   - Validate workflow pipelines
7. **Merge Sorted Lists**
   - Combine paginated API results
   - Merge logs from services
   - Aggregate sorted feeds
8. **DFS / BFS**
   - File system traversal
   - Dependency graphs
   - Shortest path (routing, recommendations)
9. **Quicksort / Mergesort**
   - Sort large datasets
   - Optimise backend performance
   - Ranking and filtering systems
10. **Palindrome Number**
   - Input validation
   - Token symmetry checks
   - Logic reasoning practice


11. Calculate Fibonacci numbers. (Naive vs. optimised approaches).
12. Find duplicates in an array (or find a missing number).
13. Compute factorial of a number (iterative/recursive).
14. Max subarray sum (Kadane’s algorithm).
15. Valid parentheses: Check if a string of (,),{,}, etc. is balanced.
16. Rotate a 2D matrix by 90 degrees.
17. Implement a stack using two queues or vice versa.
18. Find the Lowest Common Ancestor (LCA) in a binary tree.
19. Solve a Sudoku (backtracking). (Often used as a coding problem).
20. Implement memoisation or dynamic programming for a classic problem (e.g. coin change).