1. Design a URL shortening service (like bit.ly). (Handle hashing, DB mapping, scaling).
2. How would you implement rate limiting in a distributed system? (Token buckets, Redis counters).
3. Design a real-time chat application (e.g. WhatsApp/Slack). (WebSocket connections, message queues, offline storage).
4. How do you architect a Content Delivery Network (CDN)? (Edge caching, load balancing).
5. Design a microservices-based e-commerce platform. (Services for users, catalog, orders; APIs; event-driven).
6. Describe database scaling strategies for high-traffic apps. (Sharding, replication, read replicas, caching).
7. How would you process large file uploads or batch jobs? (Chunking, message queues, worker pool).
8. Implement search over a large dataset. (Use Elasticsearch or full-text indexes).
9. Design a multi-channel notification system. (Email, SMS, push; templating; retry logic).
10. Explain caching strategies at different layers (browser, CDN, app, DB). (Cache-aside, write-through, TTL).
11. Build a real-time analytics and monitoring system. (Kafka/streams, time-series DB, dashboards).
12. What is load balancing and why is it used? (Evenly distribute requests).
13. CAP Theorem: Explain Consistency, Availability, Partition Tolerance trade-offs.
14. What is an API Gateway? (Purpose in microservices).
15. Explain the sidecar pattern (e.g. in Kubernetes service mesh)..
16. What is eventual consistency? (In distributed DBs like Cassandra).
17. How would you ensure a system is highly available? (Redundancy, failover, replication).
18. What is a message queue and why use it? (Asynchronous decoupling).
19. Explain containerization (e.g. Docker) and orchestration (e.g. Kubernetes)..
20. What is middleware in a web system? (E.g. Express.js or API middleware)
